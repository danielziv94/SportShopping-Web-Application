import styled from "styled-components";

const NavBar = ()=>{
    return(
    <Container>mashu</Container>
    )
};

const Container = styled.div`
    width: 100%;
    height: 3ram;
    background-color: blue;
`

export default NavBar;